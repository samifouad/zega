/target
.target
# Cargo.lock is committed: this workspace ships a CLI and a server, so a build
# has to be reproducible and CI can pass --locked.
*.swp
*.swo
.DS_Store
/.target
/:memory:
/.tmp

# The explorer is a normal npm project inside this repo.
browser/node_modules/
browser/test-results/
browser/dist/
browser/playwright-report/
/node_modules/
/dist/
/artifacts/
/npm/consumers/*/node_modules/
/npm/consumers/*/dist/
__pycache__/
