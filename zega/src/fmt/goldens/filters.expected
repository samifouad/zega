query {
  Player(
    ((a: 1 || b != 2) && c <> 3) ||
    d >= 4 &&
    e <= 5 &&
    f > 6 &&
    g < 7 &&
    name startsExact "A" &&
    name endsExact "B" &&
    name findExact "C" &&
    name startsLike "a" &&
    name endsLike "b" &&
    name findLike "c"
  ) { name }
}
